# BLOCKED_A — A（零件资料库）遇到的卡点与事实不符

## 一、【任务 0 核对结果】任务书里的图组数目与实测不符 —— 不阻塞，已按实测继续

任务书写：「Body 48 个图组、Electrical 41、Engine/Fuel/Tool 25、Power Train/Chassis 30，共 144」。
我 2026-07-30 亲手抓同一个 URL 数了，四个数字有三个对不上。

证据（原始 HTML，非渲染后的页面）：

```
curl -s -A "Mozilla/5.0 ... Chrome/120" https://www.toyotapartsdeal.com/2022-toyota-corolla-sedan_nap-parts.html
HTTP=200 BYTES=142252

# 按 /parts-list/2022-toyota-corolla-sedan_nap/<category>/<group>.html 去重统计
Counter({'body': 42, 'electrical': 36, 'power_train_chassis': 29, 'engine_fuel_tool': 26}) TOTAL 133
```

| 部分 | 任务书 | 实测 | 差 |
|---|---|---|---|
| Body | 48 | 42 | -6 |
| Electrical | 41 | 36 | -5 |
| Engine/Fuel/Tool | 25 | 26 | +1 |
| Power Train/Chassis | 30 | 29 | -1 |
| 合计 | 144 | 133 | -11 |

**判断：不阻塞，继续做。** 理由：
1. 受影响的只有「图组总数」这个背景数字，任务书真正冻结的是 **group 的四个取值**，这四个取值实测完全确认存在（页面 category-menu 里逐字就是 Body / Electrical / Engine/Fuel/Tool / Power Train/Chassis），没有第五种。
2. 硬指标要求 `subgroup_en 去重 ≥60`，实测 133 个图组，最终用到 124 个，余量充足。
3. 差异最可能的来源：任务书那个数字是把 Sedan 与 Hatchback（或多个年份 / 多个 submodel）的图组合并数出来的，而我数的是 2022 Sedan-NAP 单一 submodel。**我没有去证实这个猜测，所以这里只把它写成猜测，不当结论。**

我按 **实测的 133** 走，不按 144 走。实测数字也写进了 `data/parts.json` 的 `vehicle.counts_note`。

---

## 二、格式冻结与真实世界对不上的三处，及我的处理

### 2.1 有些零件的官方号带内饰色后缀，超出 `^[0-9A-Z]{5}-[0-9A-Z]{5}$`

方向盘、部分外饰件的官方零件号形如 `45100-0Z120-C0`（后两位是内饰色/纹理代码）。
冻结正则只允许 5-5 两段，装不下这个后缀。

**处理**：这类件的 `oem_pn` 一律**留空**，把完整号写进 `spec` 自由文本，用户照样能拿去查。
不截断成 `45100-0Z120` 充数——虽然基础号很可能有效，但我没有独立证据证明截断后仍是可下单的有效号，
按「宁缺毋滥」不赌这一把。

### 2.2 五种轴承类型的分类装不下真实存在的两种

冻结要求 bearing 件的 spec 必须含「深沟球/角接触/圆锥滚子/滚针/轮毂单元」之一。但本车上真实存在：
- **圆柱滚子轴承**（如 90365-24001 Bearing, Cylindrical Roller）
- **推力球轴承**（如 48619-02010 Bearing, Strut Mounting，减振器支柱轴承）

这两种都不在五分类里。**处理**：不给它们打 `bearing` 标签（打了就必须在 spec 里写一个错误的类型，那是造假），
而是按它们的实际功能打 `suspension` 等标签，并在 spec 里**如实写明真实类型**并注明「不属于本库五分类」。
最终 29 条 bearing 件的类型全部有官方零件名直接背书（官方名里就写着 Tapered Roller / Needle Roller / Radial Ball），
不是我推断出来的。

### 2.3 有些「总成」在官方目录里根本不作为单件出售，因此没有零件号

冻结判定要求核心件（has_mesh=true 或命中九类 tags）必须有真实零件号。但下列总成官方只按零部件出售：
白车身、前/后悬架总成、方向盘骨架总成、前排座椅总成、后排座椅总成、头枕总成。

**处理**（三条都不涉及编号或摘标签）：
- 白车身、前/后悬架总成、方向盘总成 → 保留为层级容器，但 `has_mesh=false`。它们的几何由挂在下面的
  真实钣金件、摆臂、减振器、弹簧承担，B 照样能拼出整车，只是不把容器本身当成一个网格。
- 前排座椅总成、后排座椅总成、头枕总成 → **直接删掉这三个虚构节点**，改用官方目录里真实存在的座椅件
  （坐垫发泡 71512-02870、靠背骨架 71017-02A80、坐垫加热垫 87510-0R190、ISOFIX 支架 73705-02260、
  座椅侧气囊 73920-02350、滑轨手柄 72211-02260 等）。座椅类最终 21 条，全部有真号。

**我没有采用的两条路，以及为什么**：编一个号（明令禁止，且领导会拿去查）；
把 seat 标签摘掉让它不算核心件（属于「tags 乱贴凑数量」的反向操作，同样是作弊）。

---

## 三、如实记录的限制

- **丰田真 BOM / TIS 没有碰。** 按任务书要求，这类经销商授权资源不去找、不假装找到。
  本库的零件号与零件名全部来自 toyotapartsdeal.com 的公开图组页面（2022 Corolla Sedan-NAP，133 个图组，实抓 4833 条）。
- **零件号覆盖 416/422（98.6%）**，留空的 6 条是纯层级容器，图组仍填准，用户可拿图组名去官网自己翻。
- **suppliers 的 confidence 语义**：只有丰田原厂那一条标 `confirmed`，且 source 指向该零件号的官方零件页
  （随机抽 5 条实测全部 HTTP 200）。第二、三条供应商是行业内该品类的常见配套厂，一律标 `typical` 或 `guess`，
  note 里明写「未逐条核实该零件号」。**没有把任何 guess 标成 confirmed。**
- **爬虫与构建脚本不在 tools/ 里。** 白名单只允许我建 `tools/validate_data.py`，所以抓取、审计、构建脚本都放在
  会话临时目录的 scratchpad 下，只把结果写进 `data/`。代价：这些脚本不随交付留存。这是为了不越界，故意的。
- **一个测试环境的坑（不影响交付）**：在 `while read ... done < file` 循环里跑 curl 会全部返回 HTTP 000，
  同样的 URL 单独跑就是 200。验证 confirmed 网址时改成单条独立执行才测准，记在这里免得下次误判成链接失效。

---

## 四、其他

无其他卡点。任务 0～3 全部完成，validate_data.py 35/35 全绿，反向验证三次红转绿已在对话中贴出实际输出。
