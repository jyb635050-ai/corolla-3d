# PROGRESS_A — A（零件资料库）

## 状态：已完成。validate_data.py 35/35 全绿。

## 目标／顺序／最大风险（任务 0 完成后写，≤10 行）

1. 目标：产出 data/parts.json，≥350 条北美版 E210 卡罗拉零件，含官方零件号+官方零件名+官方图组，has_mesh=true ≥110 条给 B 建 3D。
2. 顺序：任务 1 先冻结 validate_data.py 判定 → 抓 133 个官方图组页建本地缓存 → 按 Body/Electrical/Engine/Powertrain 四部分填数据 → 任务 3 反向验证。
3. 九类点名件（轴承/电机/齿轮/座椅/门窗/电气/音响/轮毂/轮胎）优先于细节件。
4. 最大风险：**编零件号**。对策：零件号只允许从抓取到的官方页面里提取，脚本比对，抓不到就留空。
5. 次风险：role_zh 套模板灌水。对策：判定里查前 20 字唯一，且逐条按零件实际功能写。
6. 任务 0 实测：两个源都打得开（HTTP 200）；python 3.12.10 + uv 0.11.23 可跑；图组数目与任务书不符，已记 BLOCKED_A.md 最上面，判定为不阻塞。

## 最终交付数字

| 硬指标 | 要求 | 实际 |
|---|---|---|
| parts | ≥350 | **422** |
| has_mesh=true | ≥110 | **216** |
| subgroup_en 去重 | ≥60 | **124** |
| tags 含 bearing | ≥18 | **29** |
| 判定通过 | 全绿 | **35/35** |

- group 分布：body 127 / powertrain_chassis 116 / electrical 92 / engine_fuel_tool 87
- 带真实零件号：416 / 422（98.6%），其余 6 条为纯层级容器，按「宁缺毋滥」留空
- 九类覆盖：bearing 29(mesh10)、motor 23(22)、gear 122(54)、seat 21(11)、door_window 40(28)、electrical 103(55)、audio 9(6)、wheel_hub 15(10)、tire 19(8)

## validate_data.py 冻结记录

- sha256 = `e0eb7f2696532d32f3d0411b6fc341fcde0abf36e58ea8d04412e13120b8fc1b`
- 任务 1 冻结后**判定与阈值一字未改**，交付时重算 sha256 与冻结值完全一致（已在对话中贴出比对）。
- 冻结后只做过一次改动：加 `sys.stdout.reconfigure(encoding="utf-8")`，因为 Windows 控制台默认 GBK 会让含中文的判定输出直接抛 UnicodeEncodeError。该改动在冻结 sha256 之前完成，不涉及任何判定逻辑。

## 变更记录

### 2026-07-30 任务 0：核对
- toyotapartsdeal.com 索引页 HTTP 200（142252 bytes）；图组明细页 HTTP 200（206997 bytes）且含真实零件号 ✅
- en.wikipedia.org/wiki/Toyota_Corolla_(E210) ✅ 确认北美动力总成与任务书一致
- `uv run --no-project python` ✅ Python 3.12.10
- 四大 group 取值 ✅ 逐字确认（页面 category-menu），无第五种
- ❌ 图组数目对不上（任务书 144，实测 133）→ 详见 BLOCKED_A.md

### 2026-07-30 任务 1：写 validate_data.py 并冻结
- 35 条独立判定，全部打印 PASS/FAIL，任一 FAIL 则退出码 1
- 无数据时跑一遍确认会报红（[FAIL] data/parts.json 存在）

### 2026-07-30 任务 2：抓数据 + 填数据
- 抓完 133 个官方图组页 → 本地缓存 → 解析出 **4833 条**真实零件，其中 3893 条零件号为标准 5-5 格式
- 关键决定（**建议→改走更好的路**）：不凭记忆写零件号，改成**先抓后写**——所有条目的 oem_pn 必须能在抓取结果里查到，且必须出现在我声明的那个图组里，由独立脚本 `audit_pn.py` 逐条比对。
  - 这个决定当场救了命：第一批 40 个零件号里，我凭记忆写的 **13 个查无此号**（如 47730-02610、45510-02440），全部被审计脚本抓出来并换成真号（47730-02580、45510-02360…）。
  - 最终 416 个零件号 100% 通过审计：真实存在 + 图组吻合。
- name_en 一律由构建脚本从官方抓取结果直接取，不用我手写的，杜绝零件名漂移。

### 2026-07-31 收尾：修 4 条 FAIL → 全绿
第一次全量构建出 413 条时有 4 条 FAIL，处理如下（都不是靠放宽判定解决的）：
1. **13 个核心件没有零件号** —— 其中 7 个从官方目录查到了真号补上（发电机线束、前照灯、车窗主控开关、差速器壳、换挡杆、离合器主缸、后门钣金）；4 个纯层级容器（白车身、前/后悬架总成、方向盘总成）确认官方无可售零件号，因此**不编号**，改为 has_mesh=false（它们的几何由挂在下面的真实钣金/悬架件承担，不影响 B 拼整车）；3 个抽象座椅总成节点直接删除，换成真实座椅件（坐垫发泡、靠背骨架、加热垫、ISOFIX 支架、侧气囊等）。
2. **42 处 connects_to 悬空** —— 全部逐条映射到真实存在的等价零件，映射表明写在构建脚本里可审计。
3. **2 个 parent 指向不存在的 id** —— 补上两个真实零件（后门钣金 67004-02531、后隔板衬垫 63352-02030）。
4. 结果 422 条，35/35 全绿。

### 2026-07-31 任务 3：反向验证（三次红 → 还原全绿）
- 把 front_axle_hub_rh 的 oem_pn 改成 `123-456` → 报红 2 条（核心件正则 + 非空正则）✅
- 把核心件 rear_hub_bearing_rh 的 suppliers 删到 2 条 → 报红「每条 suppliers >= 3」✅
- 给 front_brake_disc 的 connects_to 加一个不存在的 id → 报红「connects_to 引用的 id 都存在」✅
- 三次全部还原后重跑：35/35 全绿，脚本 sha256 未变

## 给 B 的接口说明（不联系 B，只在这里写清楚）

- 文件：`data/parts.json`，顶层 `{"vehicle":{...},"parts":[...]}`，字段与冻结格式完全一致（校验里有「字段名不多不少」这一条）。
- 零件树：唯一根是 `body_shell`（parent 为空串），沿 parent 可走到全部 422 个节点，无环无孤点。
- 建模清单：取 `has_mesh == true` 的 216 条。若只想做领导要求的 110 条，建议优先按 group 均匀取或按九类 tags 取。
- `vehicle.counts_note` 里记了官方四大部分的实测图组数，与任务书给的数字不一致，原因见 BLOCKED_A.md。
